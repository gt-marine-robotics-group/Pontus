# Pontus "Manual" CV

This directory contains information about the traditional cv algorithms for object detection.

Important: Running code requires the correct path to Duke and Sonia training image directories. Update `IMAGE_DIR_DUKE` and `IMAGE_DIR_SONIA` in `constants.py` to the correct paths.
